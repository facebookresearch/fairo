#@package _global_
mephisto:
  blueprint:
    data_csv: ${task_dir}/data.csv
    extra_source_dir: ${task_dir}/server_files/extra_refs
    preview_source: ${task_dir}/server_files/vision_annotation_task_preview.html
    task_source: ${task_dir}/server_files/vision_annotation_task.html
    units_per_assignment: 1
  database:
    _database_type: singleton
  task:
    assignment_duration_in_seconds: 600
    task_description: Mark the blocks that make up a shape
    task_name: ca-vis-anno
    task_reward: 1.25
    task_tags: 3D, annotate, shapes
    task_title: Mark images in 3D
